{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shirube.dev/schemas/shirube-audit.schema.json",
  "title": "Shirube Audit v1",
  "description": "Canonical Shirube audit output and audit item set schema for list-driven semantic audit records.",
  "oneOf": [
    { "$ref": "#/$defs/audit_record" },
    { "$ref": "#/$defs/audit_item_set" }
  ],
  "$defs": {
    "stage": {
      "enum": [
        "stage_3_spec_audit",
        "stage_6_impl_audit",
        "stage_9_impl_to_code_audit"
      ]
    },
    "audit_record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "document_type",
        "audit_id",
        "stage",
        "reviewer_actor",
        "reviewer_model",
        "target_head",
        "item_set_ref",
        "items"
      ],
      "properties": {
        "schema_version": { "const": "shirube-audit/v1" },
        "document_type": { "const": "audit_record" },
        "audit_id": { "type": "string", "pattern": "^AUDIT-[A-Z0-9-]+$" },
        "stage": { "$ref": "#/$defs/stage" },
        "reviewer_actor": { "type": "string", "minLength": 1 },
        "reviewer_model": { "type": "string", "minLength": 1 },
        "target_head": { "type": "string", "pattern": "^[a-f0-9]{40}$" },
        "item_set_ref": { "type": "string", "minLength": 1 },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/audit_item_result" }
        }
      }
    },
    "audit_item_result": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "item_id",
        "verdict",
        "reason",
        "evidence_ref"
      ],
      "properties": {
        "item_id": { "type": "string", "minLength": 1 },
        "verdict": { "enum": ["PASS", "FAIL", "N/A", "UNVERIFIED"] },
        "reason": { "type": "string", "minLength": 1 },
        "evidence_ref": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "verdict": { "const": "FAIL" }
            },
            "required": ["verdict"]
          },
          "then": {
            "properties": {
              "evidence_ref": { "minItems": 1 }
            }
          }
        }
      ]
    },
    "audit_item_set": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "document_type",
        "item_set_id",
        "stage",
        "items"
      ],
      "properties": {
        "schema_version": { "const": "shirube-audit/v1" },
        "document_type": { "const": "audit_item_set" },
        "item_set_id": { "type": "string", "pattern": "^AUDIT-ITEM-SET-[A-Z0-9-]+$" },
        "stage": { "$ref": "#/$defs/stage" },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/audit_item" }
        }
      }
    },
    "audit_item": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "item_id",
        "criterion",
        "required_evidence"
      ],
      "properties": {
        "item_id": { "type": "string", "minLength": 1 },
        "criterion": { "type": "string", "minLength": 1 },
        "required_evidence": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
