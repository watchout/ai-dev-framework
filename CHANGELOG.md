# Changelog

## Unreleased

- Standardized public docs on `shirube` as the primary CLI command.
- Marked `framework` as a deprecated compatibility alias and added a runtime
  notice when the alias is invoked.
