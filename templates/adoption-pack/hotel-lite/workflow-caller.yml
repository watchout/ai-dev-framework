name: Shirube Rapid/Lite Gates Report

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - edited
      - labeled
      - unlabeled

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  shirube-rapid-lite-gates-report:
    name: Shirube Rapid/Lite Gates Report
    uses: "{{FRAMEWORK_REPO}}/.github/workflows/shirube-rapid-lite-reusable.yml@{{FRAMEWORK_REF_NAME}}"
    permissions:
      contents: read
      pull-requests: write
      issues: write
    with:
      framework_ref: "{{FRAMEWORK_REF}}"
      profile: hotel-lite
      report_only: true
      pr_body: "${{ github.event.pull_request.body || '' }}"
      changed_files: ""
      execution_context_ref: .shirube/execution-context.yaml
      adoption_plan_ref: .shirube/adoption-intake.yaml
      lifecycle_state_ref: .shirube/lifecycle-state.yaml
      handoff_ref: .shirube/control-handoffs/CH-001.yaml
      validation_evidence_ref: ""
      owner_decision_ref: ""
      enforcement_policy_ref: .shirube/enforcement-policy.yaml
      audit_checklist_ref: ""
      structured_audit_ref: ""
      structured_audit_comment_ref: ""
      audit_machine_evidence_ref: ""
