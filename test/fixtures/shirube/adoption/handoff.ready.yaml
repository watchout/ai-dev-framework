schema_version: shirube-control-handoff/rapid-lite/v1
mode: rapid-lite
profile: hotel-lite
control_handoff_id: CH-ADOPTION-READY-001
owner_confirmation_ref: https://github.com/owner/repo/issues/1#rps-confirmed
owner:
  role: repo_owner
  actor: watchout
next_role: implementation
cell:
  CELL-ID: CELL-ADF-V3-ADOPTION-INTAKE-001
  cell_type: docs_only
  risk_class: R2
  goal: Exercise adoption-ready fixture.
  allowed_paths:
    - docs/**
  forbidden_paths:
    - .github/workflows/**
validation:
  required_commands:
    - node scripts/shirube/check-adoption.mjs --format json
  required_evidence:
    - adoption_check_report
