---
id: VERIFY-DOC4L-020
status: Draft
traces:
  spec: [SPEC-DOC4L-020]
  impl: [IMPL-DOC4L-020]
  ops: [OPS-DOC4L-020]
---

# VERIFY: Shirube Gate Engine

## 0. Corresponding SPEC
`docs/spec/v1.2.0-gate-engine.md` / SPEC-DOC4L-020.

## 1. PR A Verification
PR A is docs-only.

Required checks:
- 4-layer docs exist and cross-link through frontmatter.
- No runtime files are changed.
- Existing focused compatibility checks pass:
  - `npm run type-check`
  - `npm run build:cli`
  - `npm test -- src/cli/lib/generate-engine.test.ts`
  - `npm test -- src/cli/lib/llm-control-design-validator.test.ts`
  - `git diff --check`

## 2. PR B Required Tests
Read-only workflow state engine tests:

| Case | Expected |
|------|----------|
| completed legacy discover session | hearing evidence present / phase may derive `hearing_complete` |
| incomplete legacy discover session | missing hearing evidence |
| missing discover session | missing hearing evidence |
| GitHub issue only | intake evidence only, not hearing completion |
| missing Goal Contract | goal status `missing`, no V0/V1 completion claim |
| draft Goal Contract | goal status `draft`, not approval evidence |
| approved Goal Contract | goal sufficient conditions appear in workflow state |
| artifact with goal trace | trace coverage includes referenced condition ids |
| artifact without goal trace | strict/public workflow reports uncovered goal trace |
| missing Context Pack | context pack status `missing`, no strict implementation start claim |
| ready Context Pack | stable id/hash, provider target, goal trace, and test expectations appear in state/evidence |
| missing provider/tool policy | tool policy status `missing`, no strict implementation start claim |
| complete AI Change Record | task, context pack, provider/tool policy, trace, tests, risks, and authority refs appear in state/evidence |
| GitHub Check projection | remote workflow summary references gate decisions and AI Change Record |
| placeholder roles | role status `setup_required` |
| role separation violation | deterministic role finding |
| `draft_only` publish policy | remote publish action blocked |
| v1 current-session | readable or migratable without data loss |
| local-only project | no GitHub requirement |
| merge-authority unavailable | release authority finding, not duplicated logic |

PR B must not add new blocking behavior to existing commands.

## 3. PR C Required Tests
Workflow observability tests:

| Command | Required behavior |
|---------|-------------------|
| `shirube workflow status --json` | emits phase, profile, evidence summary, decisions, allowed/blocked actions |
| `shirube workflow doctor --json` | reports missing evidence and remediation; diagnostic only, exits 0 for workflow `BLOCK` decisions |
| `shirube workflow check --action <action> --json` | evaluates only action-scoped decisions and exits non-zero when scoped decisions cross `--fail-on` |
| `shirube workflow explain <rule-id> --json` | explains rule, profile, decision, evidence refs, remediation |
| local-only fixture | succeeds without GitHub auth |
| missing hearing fixture | explains required hearing/import/confirmation remediation |
| missing goal fixture | explains required Goal Contract remediation |
| goal trace fixture | reports covered and uncovered sufficient conditions |
| missing context pack fixture | explains required Context Pack remediation |
| missing AI Change Record fixture | explains required AI Change Record remediation |
| GitHub Check fixture | emits projection-ready status and redacted evidence summary |
| placeholder roles fixture | explains `shirube roles set ...` remediation |
| `draft_only` fixture | explains remote publish block |
| `implementation_start` action fixture | ignores remote-publish-only blocks |
| `remote_publish` action fixture | fails on remote publish blocks |
| missing `--action` fixture | fails explicitly rather than falling back to global BLOCK |

Output should be stable enough for MCP/GitHub wrappers. Avoid exposing private reasoning traces. PR C must not connect `workflow check` to `run`, hooks, CI, GitHub Checks, issue/PR generation, or design generation.

Initial action names:
- `design_draft`
- `implementation_start`
- `implementation_split`
- `remote_publish`
- `merge`
- `release`

## 4. Compatibility Fixtures
Before broad rollout:
- existing `discover -> generate` remains green;
- #195 negation fixture remains green;
- incidental `PR` / `CI` wording does not trigger LLM Control false positives;
- Issue generation and PR generation trigger LLM Control validation;
- GitHub Issue text does not approve a Goal Contract by itself;
- a locally passing implementation without goal trace is reported as implementation-success / goal-trace-incomplete, not as completed delivery;
- a strict implementation cannot start from a generic one-line worker prompt when a task-aware Context Pack is missing;
- imported external-agent evidence preserves provider identity and does not bypass trace/audit gates;
- AI Change Record projection does not expose secret-bearing local context;
- #123 hook behavior is not made canonical;
- #91 read-receipt data maps to evidence only;
- #51 modification/coherence behavior is deferred to change-intake mapping.

## 5. Manual Dogfood Checks
Before applying beyond controlled update:
1. Run Shirube itself through `workflow status`.
2. Confirm local-only fixture works without GitHub.
3. Confirm GitHub Issue intake remains intake-only.
4. Confirm missing Goal Contract is visible as a separate readiness gap.
5. Confirm a P0 feature/task/SPEC can trace to at least one goal sufficient condition.
6. Confirm missing Context Pack and missing provider/tool policy are visible before implementation start.
7. Confirm an AI Change Record can be generated from task/evidence fixtures and projected as a redacted PR summary.
8. Confirm strict profile would block missing hearing/goal/roles/context/evidence, but observe-only surfaces do not break legacy commands.
9. Confirm `workflow check --action implementation_start` does not fail because of remote-publish-only blocks.
10. Confirm `workflow check --action remote_publish` fails on remote publish blocks.
11. Confirm status/check/explain output is understandable enough for external MCP users.

## 6. Exit Criteria For Public-Grade Claim
The Gate Engine is public-grade only after:
- PR A/B/C merged;
- Goal Contract fixture proves V0/V1 sufficient conditions are required before strict implementation/release claims;
- phase plan, feature catalog, task DAG, SPEC/IMPL readiness, Context Pack, provider/tool policy, pre-impl audit, implementation audit, AI Change Record, GitHub Check projection, post-merge verify, and goal progress fixtures pass;
- strict-mode fixtures pass;
- legacy compatibility proven;
- security/privacy projection documented;
- dogfood evidence captured on Shirube before Kodama/Totonoe broad rollout.
