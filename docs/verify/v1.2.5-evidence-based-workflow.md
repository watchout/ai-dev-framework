---
id: VERIFY-DOC4L-016
status: Draft
traces:
  spec: [SPEC-DOC4L-016]
  impl: [IMPL-DOC4L-016]
---

# VERIFY: ADF v1.2.5 — Evidence-Based Workflow Discipline (skeleton)

> Honesty: [検証済] / [文献確認] / [推測]
> 本 VERIFY は **skeleton**、impl 着手時に dev-bot が詳細追記。

## 0. 対応する SPEC / IMPL [必須]
- SPEC-DOC4L-016
- IMPL-DOC4L-016

## 1. 機能テスト (Gherkin) [必須]

### 1.1 [VERIFY-DOC4L-016-001] §Evidence 不在 → CI block
```gherkin
Given PR description に §Evidence section 不在
When evidence-check.yml trigger
Then exit 2 + PR comment "Missing §Evidence section"
```

### 1.2 [VERIFY-DOC4L-016-002] [検証済] + §Evidence 空 → block
```gherkin
Given PR description に "[検証済] X が動く"
And §Evidence 空
Then exit 2 + reason "[検証済] without evidence"
```

### 1.3 [VERIFY-DOC4L-016-003] file 引用 broken
```gherkin
Given §Evidence に "src/foo.ts:42 [content X]"
And src/foo.ts:42 が "content Y" に変更
When framework evidence verify <pr>
Then exit 2 + "src/foo.ts:42 evidence broken"
```

### 1.4 [VERIFY-DOC4L-016-004] pre-commit hook block
```gherkin
Given commit message "feat: X [検証済]"
And diff に file 引用なし
When pre-commit hook trigger
Then exit 2 (commit block)
```

### 1.5 [VERIFY-DOC4L-016-005] strict mode toggle
```gherkin
Given ADF_EVIDENCE_STRICT=0 (warn only)
And §Evidence 不在 PR
Then warn のみ、CI exit 0 (bootstrap mode)
```

## 2. 境界値テスト
| 項目 | 境界 | 期待 |
|---|---|---|
| evidence verify 実行時間 | 10s 直前 | warn なし |
| §Evidence section 文字数 | 5000 char 直前 | OK |
| 5000 char 超 | warn |

## 3. 異常系テスト
| 入力 | 期待 |
|---|---|
| §Evidence 不在 PR | exit 2 |
| URL 404 | retry 3 後 warn (block ではない、fail-open) |
| DB query unreachable | warn |
| `--no-verify` bypass | audit log + branch protection で block |

## 4. 認証/認可テスト
N/A (CI gate、bot session 内)

## 5. パフォーマンステスト
| 項目 | 基準 |
|---|---|
| evidence verify (claim 10 件) | < 10 秒 |
| pre-commit hook (claim 5 件) | < 3 秒 |

## 6. セキュリティテスト
| 攻撃ベクタ | 想定結果 |
|---|---|
| §Evidence に secret 含む PR | filter 検出 + warn (PR review で block) |
| URL に internal endpoint | strict mode で warn |
| evidence の改竄 (commit history 偽装) | git 管理 + signed commit で防御 |

## 7. Definition of Done [必須]
- [ ] Gate 0/1/2/3 全 pass
- [ ] CI green
- [ ] §Evidence section が `_template.md` に default
- [ ] evidence-check.yml が CI で稼働
- [ ] framework evidence verify CLI 動作
- [ ] pre-commit hook 動作確認 (haishin-puls-hub install)
- [ ] **haishin-puls-hub で 1 週間 dogfood**
- [ ] 期間中 §Evidence 不在 PR 0 件 (= strict mode 100% 強制成功)
- [ ] `[検証済]` false positive 検出 ≥ 3 件 (機能している証拠)
- [ ] L1 → L2 → L3 → L4 chain 完了

## 8. トレース [必須]

| VERIFY ID | SPEC FR |
|---|---|
| VERIFY-DOC4L-016-001 | FR-002 |
| VERIFY-DOC4L-016-002 | FR-002 |
| VERIFY-DOC4L-016-003 | FR-004 |
| VERIFY-DOC4L-016-004 | FR-003 |
| VERIFY-DOC4L-016-005 | FR-006 (bootstrap) |

## §Evidence (skeleton 根拠 — repo 内引用 only)

### 実 file 引用
- `docs/spec/v1.2.5-evidence-based-workflow.md` §7 受入基準 (本 VERIFY は §7 の Gherkin 詳細化)
- `docs/specs/09_ENFORCEMENT.md` §2 Bypass Audit (本 spec の `--no-verify` 連携根拠)
