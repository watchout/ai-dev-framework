---
id: VERIFY-DOC4L-010-011
status: Draft
traces:
  spec: [SPEC-DOC4L-010, SPEC-DOC4L-011]
  impl: [IMPL-DOC4L-010, IMPL-DOC4L-011]
---

# VERIFY: ADF v1.2.3 — Production-like E2E + 4-Evidence (skeleton)

> Honesty labels: [検証済] / [文献確認] / [推測]
> 本 VERIFY は **skeleton**、impl 着手時に dev-bot が詳細追記。

## 0. 対応する SPEC / IMPL [必須]
- [文献確認] SPEC-DOC4L-010+011 (`docs/spec/v1.2.3-test-layer.md`)
- [文献確認] IMPL-DOC4L-010+011 (`docs/impl/v1.2.3-test-layer.md`)

## 1. 機能テスト (Gherkin) [必須]

### 1.1 [VERIFY-DOC4L-010-001] launchd integration

```gherkin
Feature: launchd integration
  Scenario: load + start + kill + restart
    Given com.agent-comms.state-daemon.plist が installed
    When launchctl load
    Then launchctl list で running 状態
    When kill -9 daemon process
    Then ThrottleInterval+5s 以内に restart
```

### 1.2 [VERIFY-DOC4L-010-002] E2E wake verification

```gherkin
Feature: pg_notify → tmux send-keys → bot reply
  Scenario:
    Given state-daemon launchd 稼働
    And test-bot tmux session 起動
    When INSERT pending row (agent_id='test-bot')
    Then 5 秒以内に tmux pane に "check inbox" 受領
    And last_wake_attempt_at 更新
```

### 1.3 [VERIFY-DOC4L-011-001] 4-Evidence routine

```gherkin
Feature: 4-Evidence verify report
  Scenario: CTO L3 sanity
    Given commit <sha> merge 候補
    When CTO L3 verify
    Then PR comment に 4-Evidence section
    And process / DB / log / tmux 4 種 CLI 出力
    And 欠如時 merge gate failure
```

## 2. 境界値テスト [推測]
| 項目 | 境界 | 期待値 |
|---|---|---|
| E2E test 実行時間 | 60s 直前 | warn なし、exit 0 |
| 60s 超過 | timeout、retry |
| 4-Evidence 収集時間 | 30s 直前 | warn なし |

## 3. 異常系テスト [推測]
| 入力 | 期待 |
|---|---|
| launchd unavailable (Linux) | skip with marker |
| tmux session 不在 | exit 2、reason 表示 |
| 4-Evidence section 欠如 PR | merge gate exit 2 |

## 4. 認証/認可テスト
N/A

## 5. パフォーマンステスト
| 項目 | 基準 |
|---|---|
| E2E test 1 回 | < 60 秒 |
| 4-Evidence 収集 | < 30 秒 |

## 6. セキュリティテスト
- log evidence の PII filter 確認
- secret leak 検査

## 7. Definition of Done [必須]
- [ ] Gate 0/1/2/3 全 pass
- [ ] CI green (macOS runner)
- [ ] launchd / E2E wake / tmux send-keys 3 種 e2e test pass
- [ ] 4-Evidence routine が CTO L3 sanity に組込み
- [ ] haishin-puls-hub で 1 週間 dogfood (本日 #335 hotfix 後の安定確認)
- [ ] L1 → L2 → L3 → L4 chain 完了

## 8. トレース [必須]

| VERIFY ID | SPEC FR |
|---|---|
| VERIFY-DOC4L-010-001 | FR-010-1 |
| VERIFY-DOC4L-010-002 | FR-010-2 |
| VERIFY-DOC4L-011-001 | FR-011-1+2 |

## §Evidence (skeleton 根拠)

### 実 file 引用
- `docs/spec/v1.2.3-test-layer.md` SPEC §7 受入基準
- 本 VERIFY は SPEC §7 を Gherkin 詳細化したもの
