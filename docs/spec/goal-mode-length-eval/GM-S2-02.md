# SPEC: GM-S2-02 Goal-Mode Length Eval

## 0. Meta
- Origin Issue: #530
- Curve: GM-LEN-R1-2026-07-02-A
- Mode: pre-merge pilot stacked on #528 exact head `45c33e6bcee2163fa43ed014bed50c1cab9495f6`
- Bucket: S2
- Planned fixture assertions: 6
- Control points: 3

Machine-readable control points:

```yaml
control_points:
  - id: GM-S2-02-CP-01
    subject: r1_scope_guard
    authority: deterministic-script
    enforcement:
      mode: enforced
    evidence_required:
      - npm run test -- test/shirube/goal-mode-length-eval-GM-S2-02.test.ts
      - node scripts/shirube/check-script-control-fitness.mjs --file docs/spec/goal-mode-length-eval/GM-S2-02.md --format json
    script_ref: test/shirube/goal-mode-length-eval-GM-S2-02.test.ts
  - id: GM-S2-02-CP-02
    subject: fixture_manifest_count
    authority: deterministic-script
    enforcement:
      mode: enforced
    evidence_required:
      - npm run test -- test/shirube/goal-mode-length-eval-GM-S2-02.test.ts
      - node scripts/shirube/check-script-control-fitness.mjs --file docs/spec/goal-mode-length-eval/GM-S2-02.md --format json
    script_ref: test/shirube/goal-mode-length-eval-GM-S2-02.test.ts
  - id: GM-S2-02-CP-03
    subject: premerge_base_trace
    authority: deterministic-script
    enforcement:
      mode: enforced
    evidence_required:
      - npm run test -- test/shirube/goal-mode-length-eval-GM-S2-02.test.ts
      - node scripts/shirube/check-script-control-fitness.mjs --file docs/spec/goal-mode-length-eval/GM-S2-02.md --format json
    script_ref: test/shirube/goal-mode-length-eval-GM-S2-02.test.ts
```

## 1. Purpose
This trial records one R1 goal-mode implementation length sample for the frozen curve. It measures docs/spec/test-fixture work only and does not alter production delivery policy.

## 2. Required Output
The trial output is a unique spec file, a unique fixture directory, and a unique Vitest file for GM-S2-02. The fixture manifest predeclares 6 acceptance fixture assertion(s).

## 3. Safety Boundary
The trial is limited to R1 docs/spec/test fixture paths. It does not touch runtime, API, database, product, security, protected-surface code, branch protection, rulesets, required checks, owner approval, or merge state.

## 4. Classification Rules
Bucket assignment is fixed before the run from the manifest: S2. The final file count may be reported as evidence, but it must not reclassify the trial after results are known.

## 5. Acceptance Criteria
- The per-trial Vitest file passes all predeclared fixture assertions.
- The script-control fitness validator passes this spec file.
- The PR remains draft pilot evidence against codex/527-script-control-fitness.

## 6. Gate Behavior
The deterministic gate is script-control fitness. Reviewer or LLM subjective pass/fail is not accepted as trial evidence for this curve.

## 7. Scenarios
```gherkin
Given the GM-S2-02 fixture manifest declares 6 acceptance fixture assertion(s)
When the GM-S2-02 Vitest file runs
Then every declared fixture assertion is checked deterministically
```

## 8. Implementation Contract
The branch adds only GM-S2-02 R1 docs/spec/test fixture files. It starts from #528 exact head `45c33e6bcee2163fa43ed014bed50c1cab9495f6` and targets base branch `codex/527-script-control-fitness`.

## 9. Review Boundary
This is implementation-executor pilot evidence only. It does not request owner approval, merge approval, branch-protection edits, required-check edits, R2/R3 inference, or policy adoption.

## 10. 制御機構選定原則
script 選定根拠: The trial evidence must be deterministic and replayable from fixture JSON plus the Vitest assertion loop.

Hook 選定根拠: Hook 不採用 for this trial because no developer workstation or CI hook is needed to decide the fixture verdict.

GitHub 選定根拠: GitHub PR check runs are collected as external evidence, but GitHub does not replace the deterministic script verdict.

LLM boundary: LLM output may summarize the run table, but cannot change bucket assignment, thresholds, adoption rule, or pass/fail evidence.

| Requirement | Mechanism | 不可避 case 該当 (Hook のみ) | 根拠 |
|-------------|-----------|-----------------------------|------|
| Fixture assertions | Vitest unit test | - | deterministic fixture replay |
| Control-point authority | script-control validator | - | machine-readable YAML gate |
| PR evidence | GitHub check runs | - | exact-head audit trail |
| Policy boundary | report text | - | no adoption from this PR alone |

## 11. Testing Layer
The testing layer is a focused unit/regression Vitest file: `test/shirube/goal-mode-length-eval-GM-S2-02.test.ts`. It checks the predeclared fixture assertion count by executing exactly the manifest-declared assertion set.
